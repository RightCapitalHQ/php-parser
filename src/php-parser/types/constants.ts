export const enum ClassModifier {
  ModifierPublic = 1,
  ModifierProtected = 2,
  ModifierPrivate = 4,
  ModifierStatic = 8,
  ModifierAbstract = 16,
  ModifierFinal = 32,
  ModifierReadonly = 64,
}
