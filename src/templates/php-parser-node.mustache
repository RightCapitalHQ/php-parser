import type { {{{parentNodeName}}} } from '{{{parentNodeFilePath}}}'; // fullyQualifiedNodeName {{{ fullyQualifiedParentNodeName }}}

{{#uniqueImportDeclarationGeneratedStrings}}
{{{.}}}
{{/uniqueImportDeclarationGeneratedStrings}}

// eslint-disable-next-line @typescript-eslint/naming-convention
export interface {{{nodeName}}} extends Omit<{{{parentNodeName}}}, 'nodeType'> {
  {{#nodeType}}
  nodeType: '{{nodeType}}';
  {{/nodeType}}

  {{#properties}}
  ["{{name}}"] : {{typeGenerationPackage.outputTsTypeGeneratedString}};
  {{/properties}}
}

// We also need to export a symbol by using node type
// since the nodeName is possibly duplicated
// for the files need to import all nodes, we needs to use nodeType insteads
export type {{{ fullyQualifiedNodeName }}} = {{{ nodeName }}};